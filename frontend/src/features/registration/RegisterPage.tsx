import {useState,type FormEvent} from "react";
import {useAuth} from "../../app/AuthContext";
import {AppLink} from "../../app/usePathname";
type PublicPlan="athlete"|"coach";
export const PUBLIC_PLANS={athlete:{label:"Atleta",description:"Entrena, conecta tus datos deportivos y analiza tu rendimiento con TriCoach.",priceLabel:"Precio pendiente de configurar"},coach:{label:"Entrenador",description:"Trabaja con los atletas que tengas asignados y gestiona su entrenamiento.",priceLabel:"Precio pendiente de configurar"}} as const;
function message(error:unknown){const value=error instanceof Error?error.message:"";if(value.includes("registration_unavailable")||value.includes("409"))return "No se ha podido crear la cuenta con esos datos.";if(value.includes("429"))return "Demasiados intentos de registro. Espera antes de volver a intentarlo.";if(value.includes("registration_disabled")||value.includes("403"))return "El registro no está disponible en este momento.";return "No se ha podido crear la cuenta. Revisa los datos e inténtalo de nuevo."}
export function RegisterPage(){const {register}=useAuth();const [plan,setPlan]=useState<PublicPlan|null>(null),[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[sending,setSending]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setError("");if(!plan)return;if(password!==confirm){setError("Las contraseñas no coinciden.");return}if(password.length<12||password.length>1024){setError("La contraseña debe tener entre 12 y 1024 caracteres.");return}setSending(true);try{await register({display_name:name,email,password,account_plan:plan,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone||"UTC"})}catch(cause){setError(message(cause));setSending(false)}};return <main className="login-page"><section className="login-card register-card" aria-labelledby="register-title"><div className="brand login-brand"><span className="brand__mark" aria-hidden="true">T</span><span>TriCoach AI</span></div><h1 id="register-title">Crear cuenta</h1>{!plan?<fieldset className="plan-selection"><legend>¿Qué tipo de cuenta quieres crear?</legend><div className="plan-grid">{Object.entries(PUBLIC_PLANS).map(([code,item])=><article className="plan-card" key={code}><h2>{item.label}</h2><p>{item.description}</p><small>{item.priceLabel}</small><button className="button button--primary" type="button" onClick={()=>setPlan(code as PublicPlan)}>Elegir {item.label}</button></article>)}</div></fieldset>:<><p>Cuenta seleccionada: <strong>{PUBLIC_PLANS[plan].label}</strong></p>{error&&<p role="alert" className="error-banner">{error}</p>}<form onSubmit={e=>void submit(e)}><label htmlFor="register-name">Nombre visible</label><input id="register-name" required maxLength={200} autoComplete="name" value={name} onChange={e=>setName(e.target.value)}/><label htmlFor="register-email">Correo electrónico</label><input id="register-email" type="email" required autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/><label htmlFor="register-password">Contraseña</label><input id="register-password" type="password" required minLength={12} maxLength={1024} autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/><label htmlFor="register-confirm">Confirmar contraseña</label><input id="register-confirm" type="password" required autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)}/><button type="button" className="button button--ghost" onClick={()=>{setPlan(null);setError("")}}>Volver</button><button type="submit" className="button button--primary" disabled={sending}>{sending?"Creando cuenta…":"Crear cuenta"}</button></form></>}<p><AppLink to="/login">Volver a iniciar sesión</AppLink></p></section></main>}