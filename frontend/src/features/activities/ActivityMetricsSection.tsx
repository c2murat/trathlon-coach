import {useEffect,useState} from "react";
import type {ApiClient} from "../../services/apiClient";
import type {ActivityMetrics} from "../../types/api";
export function ActivityMetricsSection({activityId,client}:{activityId:string;client:ApiClient}){const [data,setData]=useState<ActivityMetrics|null>(null);const [busy,setBusy]=useState(false);const [error,setError]=useState<string|null>(null);const load=()=>{if(!client.activityMetrics)return;client.activityMetrics(activityId).then(setData).catch(()=>setError("No se han podido cargar las métricas."))};useEffect(load,[activityId,client]);const recalc=async()=>{if(!client.recalculateMetrics)return;setBusy(true);setError(null);try{setData(await client.recalculateMetrics(activityId))}catch{setError("No se han podido recalcular las métricas.")}finally{setBusy(false)}};return <section className="summary-section factual-metrics" aria-labelledby="factual-metrics-title"><div className="enrichment-heading"><div><h2 id="factual-metrics-title">Métricas factuales</h2><p>Datos observados y cálculos deterministas · algoritmo {data?.algorithm_version??"0.6c.1"}</p></div><button className="button button--secondary" type="button" onClick={()=>void recalc()} disabled={busy||!client.recalculateMetrics}>{busy?"Calculando…":"Recalcular métricas"}</button></div>{error&&<p role="alert">{error}</p>}{data&&<dl className="enriched-grid">{data.metrics.map(m=><div key={m.key}><dt>{m.label}</dt><dd>{m.status==="available"||m.status==="partial"?<>{m.value} <small>{m.unit}</small></>:<span>Sin datos</span>}</dd><small>{m.status} · {m.source??"Sin fuente"}{m.coverage_ratio!==null?` · Cobertura ${Math.round(m.coverage_ratio*100)}%`:""}</small>{m.unavailable_reason&&<small>{m.unavailable_reason}</small>}</div>)}</dl>}</section>}