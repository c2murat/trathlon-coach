export function ActivityPagination({page,total,limit,onPage}:{page:number;total:number;limit:number;onPage(page:number):void}){const pages=Math.max(1,Math.ceil(total/limit)),start=total?(page-1)*limit+1:0,end=Math.min(page*limit,total);return <nav className="activity-pagination" aria-label="Paginación de actividades"><p>{start}–{end} de {total.toLocaleString("es-ES")} · Página {page} de {pages}</p><div><button className="button button--secondary" disabled={page<=1} onClick={()=>onPage(page-1)}>Anterior</button><button className="button button--secondary" disabled={page>=pages} onClick={()=>onPage(page+1)}>Siguiente</button></div></nav>}
