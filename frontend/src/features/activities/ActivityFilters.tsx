import { displaySport } from "../../utils/format";import type { ActivityFilterOptions } from "../../types/api";import type { ActivityFilterState } from "./activityQuery";
export function ActivityFilters({value,options,onChange,onApply,onClear}:{value:ActivityFilterState;options:ActivityFilterOptions|null;onChange(v:ActivityFilterState):void;onApply():void;onClear():void}){const set=(key:keyof ActivityFilterState,next:string|boolean)=>onChange({...value,[key]:next});return <form className="activity-filters" onSubmit={e=>{e.preventDefault();onApply()}}><label>Buscar<input value={value.search} onChange={e=>set("search",e.target.value)} placeholder="Nombre de actividad"/></label><label>Deporte<select value={value.sport} onChange={e=>set("sport",e.target.value)}><option value="">Todos</option>{options?.sport_types.map(x=><option key={x} value={x}>{displaySport(x)}</option>)}</select></label><label>Desde<input type="date" value={value.date_from} onChange={e=>set("date_from",e.target.value)}/></label><label>Hasta<input type="date" value={value.date_to} onChange={e=>set("date_to",e.target.value)}/></label><label>Distancia mínima (m)<input type="number" min="0" value={value.min_distance_metres} onChange={e=>set("min_distance_metres",e.target.value)}/></label><label>Distancia máxima (m)<input type="number" min="0" value={value.max_distance_metres} onChange={e=>set("max_distance_metres",e.target.value)}/></label><label>Visibilidad<select value={value.visibility} onChange={e=>set("visibility",e.target.value)}><option value="">Todas</option>{options?.visibility_values.map(x=><option key={x}>{displaySport(x)}</option>)}</select></label><label className="check"><input type="checkbox" checked={value.trainer} onChange={e=>set("trainer",e.target.checked)}/> Entrenador</label><label className="check"><input type="checkbox" checked={value.manual} onChange={e=>set("manual",e.target.checked)}/> Manual</label><div className="filter-actions"><button className="button button--secondary" type="button" onClick={onClear}>Limpiar filtros</button><button className="button button--primary" type="submit">Aplicar filtros</button></div></form>}


