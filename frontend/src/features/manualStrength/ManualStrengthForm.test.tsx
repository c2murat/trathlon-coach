import {render,screen} from "@testing-library/react";import userEvent from "@testing-library/user-event";import {describe,expect,it,vi} from "vitest";import {ManualStrengthForm} from "./ManualStrengthForm";
describe("ManualStrengthForm",()=>{it("renders and validates required regions",async()=>{const save=vi.fn();render(<ManualStrengthForm saving={false} onCancel={vi.fn()} onSave={save}/>);expect(screen.getByLabelText("Fecha")).toBeInTheDocument();expect(screen.getByLabelText("Duración (minutos)")).toBeInTheDocument();expect(screen.getByRole("group",{name:"Partes del cuerpo trabajadas"})).toBeInTheDocument();expect(screen.getByRole("button",{name:"Cerrar formulario"})).toBeInTheDocument();expect(screen.getByText("0/2000 caracteres")).toBeInTheDocument();await userEvent.click(screen.getByRole("button",{name:"Guardar entrenamiento"}));expect(await screen.findByRole("alert")).toHaveTextContent(/parte del cuerpo/);expect(save).not.toHaveBeenCalled()});it("creates a valid session and applies region rules",async()=>{const save=vi.fn();render(<ManualStrengthForm saving={false} onCancel={vi.fn()} onSave={save}/>);await userEvent.click(screen.getByRole("button",{name:"Espalda"}));await userEvent.click(screen.getByRole("button",{name:"Brazos"}));await userEvent.click(screen.getByRole("button",{name:"7"}));await userEvent.click(screen.getByRole("button",{name:"Guardar entrenamiento"}));expect(save).toHaveBeenCalledWith(expect.objectContaining({body_regions:["back","arms"],perceived_exertion:7,duration_minutes:45}))});it("shows Zona media while preserving the core API value and updates the notes counter",async()=>{const save=vi.fn();render(<ManualStrengthForm saving={false} onCancel={vi.fn()} onSave={save}/>);await userEvent.click(screen.getByRole("button",{name:"Zona media"}));await userEvent.type(screen.getByRole("textbox",{name:/Notas/}),"abc");expect(screen.getByText("3/2000 caracteres")).toBeInTheDocument();await userEvent.click(screen.getByRole("button",{name:"Guardar entrenamiento"}));expect(save).toHaveBeenCalledWith(expect.objectContaining({body_regions:["core"],notes:"abc"}))});it("disables save while submitting",()=>{render(<ManualStrengthForm saving onCancel={vi.fn()} onSave={vi.fn()}/>);expect(screen.getByRole("button",{name:"Guardando…"})).toBeDisabled()})});
