import type {ManualStrengthSession} from "../../types/manualStrength";import {formatBodyRegions,formatRpe,formatStrengthDate,formatStrengthLoad,formatStrengthQuality,formatStrengthWarning} from "./manualStrengthFormat";
export function ManualStrengthSessionCard({session,busy,onEdit,onRecalculate,onDelete}:{session:ManualStrengthSession;busy?:boolean;onEdit():void;onRecalculate():void;onDelete():void}){return <article className="strength-card"><header><div><h2>Fuerza · {session.duration_minutes} min</h2><p>{formatBodyRegions(session.body_regions)}</p></div><strong>{formatStrengthLoad(session.training_load.load_value)} puntos</strong></header><dl><div><dt>Esfuerzo percibido</dt><dd>{formatRpe(session.perceived_exertion)}</dd></div><div><dt>Cálculo</dt><dd>{formatStrengthQuality(session.training_load.quality)}</dd></div></dl><time dateTime={session.started_at}>{formatStrengthDate(session.started_at)}</time>{session.notes&&<p className="strength-card__notes">{session.notes}</p>}{session.training_load.warnings.map(w=><p className="strength-warning" key={w}>{formatStrengthWarning(w)}</p>)}<footer><button onClick={onEdit}>Editar</button><button disabled={busy} onClick={onRecalculate}>{busy?"Recalculando…":"Recalcular"}</button><button onClick={onDelete}>Eliminar</button></footer></article>}
