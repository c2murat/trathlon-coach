import {describe,expect,it} from "vitest";import {getPerformanceReferenceLabel,getReferenceOriginLabel,getReferenceQualityLabel,formatReferenceValue} from "./referenceFormat";
describe("referencias",()=>{it("traduce métricas, origen y calidad",()=>{expect(getPerformanceReferenceLabel("cycling","power")).toBe("FTP");expect(getReferenceOriginLabel("manual")).toBe("Introducida manualmente");expect(getReferenceQualityLabel("high")).toBe("Alta");expect(getReferenceOriginLabel("unknown")).not.toContain("unknown")});it("formatea ritmos",()=>{expect(formatReferenceValue("running","pace",270)).toBe("4:30 min/km");expect(formatReferenceValue("swimming","swim_pace",120)).toBe("2:00 min/100 m")})});
